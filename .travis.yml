sudo: required

services:
  - docker

  before_install:
    - "docker build -t pieceofchum/docker-react -f Dockerfile.dev ."

  script:
    - "docker run pieceofchum/docker-react npm run test -- --coverage"
